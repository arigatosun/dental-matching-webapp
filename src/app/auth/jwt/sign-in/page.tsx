import { JwtSignInView } from '@/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = { title: `Sign in thoot` };

export default function Page() {
  return <JwtSignInView />;
}
