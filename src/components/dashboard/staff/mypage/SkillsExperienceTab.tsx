import React from 'react';
import { Typography, Box } from '@mui/material';

export default function SkillsExperienceTab() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">スキル・経歴</Typography>
      <Typography>ここにスキルと経歴情報が表示されます。</Typography>
    </Box>
  );
}